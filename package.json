{
  "name": "ryne",
  "version": "1.0.0",
  "private": true,
  "description": "Auth & Onboarding + Time MVP monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:typedoc": "bash -c 'mkdir -p site/typedoc && (cd client && npx typedoc --tsconfig tsconfig.json --entryPoints src --entryPointStrategy expand --skipErrorChecking --out ../site/typedoc/client) && (cd server && npx typedoc --tsconfig tsconfig.json --entryPoints src --entryPointStrategy expand --skipErrorChecking --out ../site/typedoc/server)'",
    "docs:openapi": "redocly build-docs openapi.yaml -o site/api",
    "docs:site": "bash scripts/make-docs-index.sh",
    "docs": "rimraf site && npm run docs:typedoc && npm run docs:openapi && npm run docs:site",
    "deploy:rules:dev": "cp firebase/firestore.rules.dev firebase/firestore.rules && npx firebase --project shyft-xphli --config firebase.json deploy --only firestore:rules",
    "deploy:rules:prod": "cp firebase/firestore.rules.prod firebase/firestore.rules && npx firebase --project shyft-xphli --config firebase.json deploy --only firestore:rules",
    "dev:api": "npm --workspace=@ryne/api run dev",
    "dev:web": "npm --workspace=@ryne/web run dev",
    "dev": "npm run dev:api & npm run dev:web",
    "build": "npm --workspace=@ryne/api run build && npm --workspace=@ryne/web run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/top-shelf-service/ryne.git"
  },
  "bugs": {
    "url": "https://github.com/top-shelf-service/ryne/issues"
  },
  "homepage": "https://github.com/top-shelf-service/ryne#readme",
  "license": "ISC",
  "devDependencies": {
    "@redocly/cli": "^2.0.5",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.4",
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "firebase-tools": "^14.12.1",
    "rimraf": "^6.0.1",
    "semantic-release": "^24.2.7",
    "typedoc": "^0.28.10",
    "wrangler": "^4.31.0"
  }
}
